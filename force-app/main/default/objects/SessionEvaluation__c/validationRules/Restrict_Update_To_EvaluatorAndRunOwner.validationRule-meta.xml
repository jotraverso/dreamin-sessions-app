<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Restrict_Update_To_EvaluatorAndRunOwner</fullName>
    <active>true</active>
    <description>Prevent updates to SessionEvaluation__c unless the running user is the Evaluator (Evaluator__c)</description>
    <errorConditionFormula>AND(
            NOT(ISNEW()),
            NOT(
                AND(
                    $User.Id = Evaluator__c,
                    $User.Id = Run__r.Evaluator__c
                )
            )
        )</errorConditionFormula>
    <errorDisplayField>Evaluator__c</errorDisplayField>
    <errorMessage>Only the assigned Evaluator can update this record.</errorMessage>
</ValidationRule>
